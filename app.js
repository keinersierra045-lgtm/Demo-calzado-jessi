let cart=[];const prices={'Velocity One': 429900, 'Core Tracksuit': 319900, 'Street Runner': 389900, 'Pulse Essential': 279900};function add(n){cart.push(n);render();document.getElementById("drawer").classList.add("on");document.getElementById("shade").classList.add("on")}function render(){count.textContent=cart.length;items.innerHTML=cart.length?cart.map((x,i)=>`<div class="cartrow"><span>${x}</span><b>$${prices[x].toLocaleString("es-CO")}</b></div>`).join(""):"<p>Tu bolsa está vacía.</p>";total.textContent="$"+cart.reduce((a,x)=>a+prices[x],0).toLocaleString("es-CO")}function openCart(){drawer.classList.toggle("on");shade.classList.toggle("on")}function checkout(){if(!cart.length)return alert("Agrega un producto primero.");alert("Flujo de checkout listo. Para cobrar dinero real se conecta la cuenta del comercio con la pasarela elegida.")}render();